<h1>Listing replacements</h1>

<table>
  <thead>
  <tr>
    <th>Label</th>
    <th>Template</th>
    <th>Value</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @replacements.each do |replacement| %>
      <tr>
        <td><%= replacement.label %></td>
        <td><%= truncate(replacement.template.content, length: 40) %> </td>
        <td><%= truncate(replacement.value.content, length: 40) %> </td>
        <td>
          <%= link_to 'Show', replacement %>
          <%= link_to 'Edit', edit_replacement_path(replacement) %>
          <%= link_to 'Destroy', replacement, method: :delete, data: {confirm: 'Are you sure?'} %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Replacement', new_replacement_path %>
