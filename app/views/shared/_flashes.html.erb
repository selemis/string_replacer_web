<% flash.each do |key, value| %>
    <% bootstrap = {notice: :success, alert: :danger} %>
    <% cls = bootstrap[key.to_sym] ? bootstrap[key.to_sym] : :warning %>
    <%= content_tag('div', value, class: "alert alert-#{cls}", role: 'alert') %>
<% end %>
